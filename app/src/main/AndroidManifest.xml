<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xd.ihsan.coffee">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".Coffee"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Coffee">
        <activity
            android:name=".view.activities.home.DetailActivity"
            android:exported="false"
            android:theme="@style/DialogStyle"/>
        <activity
            android:name=".view.activities.home.HomeActivity"
            android:exported="false"
            android:theme="@style/Theme.Coffee.NoActionBar" />
        <activity
            android:name=".view.activities.login.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.Coffee.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>